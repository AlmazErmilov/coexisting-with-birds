{
  "private": true,
  "scripts": {
    "test": "vitest run && npx playwright test --config tests/playwright.config.js",
    "test:unit": "vitest run",
    "test:e2e": "npx playwright test --config tests/playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0",
    "vercel": "^50.25.4",
    "vitest": "^4.0.0"
  }
}
